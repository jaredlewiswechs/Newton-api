# Newton Supercomputer Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# GUMROAD INTEGRATION (Required for payment processing)
# =============================================================================

# Your Gumroad Product ID
# Find this in your Gumroad product settings
# Example: abc123XYZ
GUMROAD_PRODUCT_ID=

# Your Gumroad Access Token
# Get this from: https://app.gumroad.com/settings/advanced#application-form
# Create an application to get your access token
GUMROAD_ACCESS_TOKEN=

# Webhook Secret (optional but recommended)
# Set this in your Gumroad product's Ping settings
# Used to verify webhook signatures
GUMROAD_WEBHOOK_SECRET=

# =============================================================================
# NEWTON SETTINGS (Optional)
# =============================================================================

# Server port (default: 8000)
PORT=8000

# Storage directory for Vault and Ledger
NEWTON_STORAGE=/tmp/newton

# Enable API key authentication (for production)
NEWTON_AUTH_ENABLED=false

# Comma-separated list of valid API keys (if auth enabled)
# NEWTON_API_KEYS=key1,key2,key3

# Rate limiting (requests per minute per IP, default: 300)
RATE_LIMIT_RPM=300

# =============================================================================
# AWS DEPLOYMENT (for Lambda + API Gateway free tier)
# =============================================================================

# AWS region (default: us-east-1 for free tier)
AWS_REGION=us-east-1

# Set via `aws configure` or export these:
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
